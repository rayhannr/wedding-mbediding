---
import { twMerge } from 'tailwind-merge'

interface Props {
  className?: string
}

const { className } = Astro.props
---

<ul class={twMerge('grid lg:grid-cols-2 gap-y-12 gap-x-8 xl:gap-x-12', className)}>
  <slot />
</ul>
